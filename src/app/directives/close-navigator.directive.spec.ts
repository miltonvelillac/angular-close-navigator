import { CloseNavigatorDirective } from './close-navigator.directive';

describe('CloseNavigatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseNavigatorDirective();
    expect(directive).toBeTruthy();
  });
});
